<nav class="navbar navbar-expand-lg navbar-light " style="background-color:#96B1C1 ; ">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('app_home')}}"><img src="{{asset('img/logo.png')}}" width="200" alt=""></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
			aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item active">
					<a {% if "app_home" == route %} class="nav-link active" {% else %} class="nav-link" {% endif %}
						href="{{path('app_home')}}">Home</a>
				</li>
				<li class="nav-item active">
					<a {% if "app_library" == route or 
							"classification" in route or 
							"author" in route or "book" in route %} 
							class="nav-link active" 
						{% else %} class="nav-link" 
						{% endif %}
						href="{{path('app_library')}}">Voltaire Library</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('app_library')}}">Voltaire Library</a>
				</li>
				<li class="nav-item">
					<a {% if "app_contact" == route %} class="nav-link active" {% else %} class="nav-link" {% endif %}
						 href="{{path('app_contact')}}">Contact</a>
				</li>
{# 
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
						data-bs-toggle="dropdown" aria-expanded="false">
						Dropdown
					</a>
					<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
						<li><a class="dropdown-item" href="#">Action</a></li>
						<li><a class="dropdown-item" href="#">Another action</a></li>
						<li>
							<hr class="dropdown-divider">
						</li>
						<li><a class="dropdown-item" href="#">Something else here</a></li>
					</ul>
				</li>
				<li class="nav-item">
					<a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
				</li> #}

			</ul>
			
		</div>
	</div>
</nav>
{% if "app_home" in route %}
<div>
	<img src="{{asset('img/wall.png')}}" style="width:100%;" alt="">
</div>
{% endif %}